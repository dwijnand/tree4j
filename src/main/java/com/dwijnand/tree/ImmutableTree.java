package com.dwijnand.tree;

/**
 * A immutable tree structure or arborescence.
 * <p>
 * This is only a marker interface, to indicate that the tree is immutable.
 * 
 * @param <T> the types of the nodes in the tree
 */
public interface ImmutableTree<T> extends Tree<T> {

}
